version: '3.9'

services:
  core: 
    build:
      context: .
      restart: always
    environment:
      - SQLITE3_DB=db.sqlite3
    depends_on:
      - db
  db:
    image: nouchka/sqlite3:latest
    volumes:
      - ./data/db:/root/db
    environment:
      - SQLITE3_DB=db.sqlite3